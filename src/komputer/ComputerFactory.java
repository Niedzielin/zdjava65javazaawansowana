/*
package komputer;

*/
/**
 * Created by Hyperbook on 10/10/2020
 **//*

public class ComputerFactory {
    private GraphicCard graphicCard;
    private PowerSuply powerSuply;
    private Processor processor;

    public ComputerFactory(String graphgicName, String powerSuplyName, String processorName) {
        this.graphicCard = new graphicCard(graphgicName);
        this.powerSuply = new powerSuply(powerSuplyName);
        this.processor = new processor(processorName);
    }
    public static Computer createComputer((String graphgicName, String powerSuplyName, String processorName) {
        GraphicCard graphicCard = new graphicCard(graphgicName);
        PowerSuply powerSuply = new powerSuply(powerSuplyName);
        Processor processor = new processor(processorName);
        return new Computer();
    }
    public Computer createComputer(){
        return new Computer(graphicCard,powerSuply,processor);
    }
}
*/
